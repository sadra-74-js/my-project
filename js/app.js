const login = () => {
  return true;
};

const register = () => {
  return true;
};

function getme(token) {
  return User;
}

function User () {
  return true;
}

// ***

const Login = (user) => {
  if (user.name === "amin") {
    return true;
  }

  return false;
};

const Register = (user) => {
  return { user, token };
};

const getMe = (token) => {
  return user;
};

// Auth